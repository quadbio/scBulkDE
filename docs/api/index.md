# API

scBulkDE follows the scanpy module structure with `pp` (preprocessing),
`tl` (tools), and utility classes/functions.

Import scBulkDE as:

```python
import scbulkde as scb
```

```{toctree}
:maxdepth: 2

preprocessing.md
tools.md
engines.md
classes.md
settings.md
```
